spring:

weather:
  api:
    api-key: ${WEATHER_API_KEY:your-dev-api-key-here}  # This reads from .env
    timeout: 30s
    # ... other weather properties

# Logging (if needed)
logging:
  level:
    root: INFO
    com.yashaswi.weatherapi: DEBUG
    org.springframework.web: DEBUG
