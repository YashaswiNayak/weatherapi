spring:
  docker:
    compose:
      enabled: false

weather:
  api:
    api-key: ${WEATHER_API_KEY:your-dev-api-key-here}
    timeout: 30s  # Longer timeout for debugging

logging:
  level:
    root: INFO
    com.yashaswi.weatherapi: DEBUG
    org.springframework.web: DEBUG
